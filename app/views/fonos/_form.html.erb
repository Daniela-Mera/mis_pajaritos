<%= form_with(model: fono) do |form| %>
  <% if fono.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(fono.errors.count, "error") %> prohibited this fono from being saved:</h2>

      <ul>
        <% fono.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :numero, style: "display: block" %>
    <%= form.number_field :numero, min:0 %>
  </div>

  <div>
    <!--<%= form.label :user_id, style: "display: block" %>-->
    <!-- antes, para modificar cualquier telefono-->
    <!--<%= form.number_field :user_id %> -->
    <!-- despues, para modificar telefono del usuario activo -->
    <!--<%= form.number_field :user_id, :value => current_user.id %>-->
    <!-- debo ocultar el campo, asi no podra modificarse -->
    <%= form.hidden_field :user_id, :value => current_user.id %>
  </div>

  <br>
  <br>
  <div>
    <%= form.submit %>
  </div>
<% end %>
