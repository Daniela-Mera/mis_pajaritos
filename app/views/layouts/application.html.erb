<!DOCTYPE html>
<html>
  <head>
    <!-- Required meta tags -->
      <meta name="viewport" content="width=device-width,initial-scale=1">
      <%= csrf_meta_tags %>
      <%= csp_meta_tag %>

    <!-- Bootstrap CSS -->
      <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    
    <!-- assets/stylesheets -->
     <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>

    <!-- Titulo -->
      <title>Los Pajaritos de Daniela</title>
</head>

  <body>
    <!--title-->
    <div class="container">
      <br>
      <h1 class="display-2">Catálogo de aves chilenas</h1>
      <p class="notice"><%= notice %></p>
      <p class="alert"><%= alert %></p>
    </div>

    <!-- Navbar -->
    <div class="container">
      <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container-fluid">
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarNavDropdown">
            <a class="navbar-brand" href="/">Inicio</a>
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
              <li class="nav-item"><a class="nav-link active" aria-current="page" href="/vertodas">Ver todas</a></li>
              <!-- Menu desplegable con opciones de usuario -->
              <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" role="button" data-bs-toggle="dropdown" aria-expanded="false">Menú de usuario</a>
                <ul class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
                  <% if user_signed_in? %>
                    <span class="dropdown-item disabled">Bienvenid@ <%= current_user.name %> <%= current_user.ap_paterno %></span>
                    <span class="dropdown-item disabled"><%= current_user.email %></span>
                    <li><hr class="dropdown-divider"></li>                  
                    <li><a class="dropdown-item" href="/users/edit">Editar datos</a></li>
                    <li><a class="dropdown-item" href="/fonos">Agregar teléfono</a></li>
                    <li><a class="dropdown-item" href="/vertodas">Mis aves</a></li>
                    <li><hr class="dropdown-divider"></li>
                    <li><a class="dropdown-item" href="/users/sign_out">Cerrar Sesión</a></li>
                  <% else %>
                    <li><a class="dropdown-item" href="/users/sign_in">Iniciar sesion</a></li>
                    <li><a class="dropdown-item" href="/users/sign_up">Registrarse</a></li>                  
                  <% end %>
                </ul>
              </li>
            </ul>
            <form class="d-flex">
              <!-- Input y boton de busqueda -->
                <input class="form-control me-2 " type="search" placeholder="pronto..." aria-label="Search">
                <button class="btn " type="submit">Buscar</button>
            </form>
          </div>
        </div>
      </nav>
      <br>
    </div>
    <!-- Ver información del usuario activo 
      <div class="container">
        <% if user_signed_in? %>
          <p>Bienvenido: <%= current_user.name %> <%= current_user.ap_paterno %></p>
          <p>Usuario: <%= current_user.email %></p>
          <br>
          <%= link_to "Cerrar Sesion", destroy_user_session_path %>
        <% else %>
          <p>Debe iniciar sesión para continuar</p>
          <%= link_to "Iniciar Sesión", new_user_session_path %>
        <% end %>
      </div>
    -->

    <!-- yield -->
    <div class="container">
        <%= yield %>
    </div>
  
    <!--footer-->
    <div class="container">
      <div class="row text-center" style="background: #f3f3f3">
        <span>Mayo 2022 | Ideado por Daniela mientras aprendía a usar API's con RoR</span>
      </div>
      <br>
      <br>
    </div>
         
  <!-- Option 1: Bootstrap Bundle with Popper -->
      <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
  </body>

</html>
